{"name":"Discord Integration - Generic","author":"Mad","supportedGames":["other"],"versions":[{"tag":"latest","description":"Connect chat to other services like Discord.","configSchema":"{\"$schema\":\"http://json-schema.org/draft-07/schema#\",\"type\":\"object\",\"required\":[\"discordChannelIdCustom\"],\"additionalProperties\":false,\"properties\":{\"sendPlayerConnected\":{\"title\":\"sendPlayerConnected\",\"description\":\"Send a message when a player connects.\",\"default\":true,\"type\":\"boolean\"},\"sendPlayerDisconnected\":{\"title\":\"sendPlayerDisconnected\",\"description\":\"Send a message when a player disconnects.\",\"default\":true,\"type\":\"boolean\"},\"onlyGlobalChat\":{\"title\":\"onlyGlobalChat\",\"description\":\"Only relay messages from global chat. (no team chat or private messages)\",\"default\":true,\"type\":\"boolean\"},\"discordChannelIdCustom\":{\"title\":\"discordChannelIdCustom\",\"description\":\"Custom Discord Channel Id\",\"type\":\"string\"},\"newPlayerDiscordChannel\":{\"title\":\"newPlayerDiscordChannel\",\"description\":\"Discord channel ID for new player announcements (leave empty to disable)\",\"default\":\"\",\"type\":\"string\"},\"newPlayerDiscordMessage\":{\"title\":\"newPlayerDiscordMessage\",\"description\":\"New player Discord message. Use {player} and {server} as placeholders.\",\"default\":\"🎆 **{player}** is a new player. Welcome to **{server}**!\",\"type\":\"string\",\"minLength\":1,\"maxLength\":200}}}","uiSchema":"{}","commands":[],"hooks":[{"function":"import { takaro, data } from '@takaro/helpers';\n\nasync function main() {\n    const { gameServerId, player, module } = data;\n\n    // Fetch the game server details\n    const gameServer = await takaro.gameserver.gameServerControllerGetOne(gameServerId);\n    const serverName = gameServer.data.data.name;\n\n    const discordChannel = module.userConfig.discordChannelIdCustom;\n\n    await takaro.discord.discordControllerSendMessage(discordChannel, {\n        message: `👋 Bye **${player.name}**. \n        **${serverName}** will miss you, come back soon!`\n    });\n}\n\nawait main();","name":"PlayerDisconnected","description":null,"eventType":"player-disconnected","regex":""},{"function":"import { data, takaro } from '@takaro/helpers';\n\nasync function main() {\n    // Parse player name from log message\n    const logMessage = data.eventData?.msg;\n    if (!logMessage) {\n        console.log('PlayerDied: No log message found in event data.');\n        return;\n    }\n    \n    // Extract player name from: \"[PrismaCore]playerDied: Wyked (Steam_76561198118541500) died @ 2445 52 -353\"\n    const nameMatch = logMessage.match(/playerDied:\\s+([^(]+)\\s+\\(/);\n    if (!nameMatch || !nameMatch[1]) {\n        console.log('PlayerDied: Could not parse player name from log message:', logMessage);\n        return;\n    }\n    \n    const playerName = nameMatch[1].trim();\n    \n    const discordChannel = data.module.userConfig.discordChannelIdCustom;\n    await takaro.discord.discordControllerSendMessage(discordChannel, {\n        message: `☠️ **${playerName}** *died*`,\n    });\n}\n\nawait main();","name":"PlayerDied","description":"No","eventType":"log","regex":".*playerDied.*"},{"function":"import { takaro, data } from '@takaro/helpers';\n\nasync function main() {\n    const discordChannel = data.module.userConfig.discordChannelIdCustom;\n    const serverStatus = data.eventData.status;\n\n    if (serverStatus === 'online') {\n        await takaro.discord.discordControllerSendMessage(discordChannel, {\n            message: `🟢 **Server Online**`,\n        });\n    } else if (serverStatus === 'offline') {\n        await takaro.discord.discordControllerSendMessage(discordChannel, {\n            message: `🔴 **Server Offline**`,\n        });\n    }\n}\n\nawait main();","name":"Online - Offline","description":"No","eventType":"server-status-changed","regex":""},{"function":"import { takaro, data } from '@takaro/helpers';\nasync function main() {\n    if (data.eventData.msg.startsWith('/'))\n        return;\n    if (data.eventData.msg.startsWith('!'))\n        return;\n    if (data.eventData.msg.startsWith('$'))\n        return;\n    const sender = data.player ? data.player.name : 'Non-player';\n    if (sender === 'Non-player')\n        return;\n    const onlyGlobal = data.module.userConfig.onlyGlobalChat;\n    if (onlyGlobal && data.eventData.channel !== 'global')\n        return;\n    const discordChannel = data.module.userConfig.discordChannelIdCustom;\n    const message = `**${sender}**: ${data.eventData.msg}`;\n    await takaro.discord.discordControllerSendMessage(discordChannel, {\n        message: message,\n    });\n}\nawait main();\n//# sourceMappingURL=GameToDiscord.js.map","name":"GameToDiscord","description":null,"eventType":"chat-message","regex":""},{"function":"import { takaro, data } from '@takaro/helpers';\nasync function main() {\n    if (data.eventData.author.isBot)\n        return;\n\n    const displayName = data.eventData.author.displayName;\n    const message = data.eventData.msg;\n\n    await takaro.gameserver.gameServerControllerSendMessage(data.gameServerId, {\n        message: `${displayName}: ${message}`,\n    });\n}\nawait main();","name":"DiscordToGame","description":null,"eventType":"discord-message","regex":""},{"function":"import { takaro, data } from '@takaro/helpers';\n\nasync function main() {\n    const { player, gameServerId, module: mod } = data;\n    const discordChannel = mod.userConfig.discordChannelIdCustom;\n    const playtimeHours = Math.floor(player.playtimeSeconds / 3600);\n\n    // Check if player has been seen before\n    const seenCheck = await takaro.variable.variableControllerSearch({\n        filters: {\n            key: ['seenPlayer'],\n            playerId: [player.id],\n            moduleId: [data.module.moduleId]\n        }\n    });\n\n    if (seenCheck.data.data.length === 0) {\n        // New player - set variable and announce to Discord\n        await takaro.variable.variableControllerCreate({\n            key: 'seenPlayer',\n            value: 'true',\n            playerId: player.id,\n            moduleId: data.module.moduleId\n        });\n\n        const newPlayerChannel = mod.userConfig.newPlayerDiscordChannel;\n        if (newPlayerChannel) {\n            const serverDetails = await takaro.gameserver.gameServerControllerGetOne(gameServerId);\n            const serverName = serverDetails.data.data.name;\n\n            const messageTemplate = mod.userConfig.newPlayerDiscordMessage || '🎆 **{player}** is a new player. Welcome to **{server}**!';\n            const message = messageTemplate.replace('{player}', player.name).replace('{server}', serverName);\n\n            await takaro.discord.discordControllerSendMessage(newPlayerChannel, {\n                message: message,\n            });\n        }\n    }\n\n    await takaro.discord.discordControllerSendMessage(discordChannel, {\n        message: `🎆 **${player.name}** connected \\n       🎮 **Playtime:** ${playtimeHours} hours`,\n    });\n}\n\nawait main();","name":"PlayerConnected","description":null,"eventType":"player-connected","regex":""}],"cronJobs":[{"function":"import { takaro, data } from '@takaro/helpers';\nimport { getServerStatusMessage, getOnlinePlayers } from './utils.js';\n\nasync function main() {\n    try {\n        const { gameServerId, module: mod } = data;\n        const discordChannel = data.module.userConfig.discordChannelIdCustom;\n\n        if (!discordChannel) {\n            console.error(\"Discord channel ID not configured\");\n            return { success: false, reason: \"Discord channel ID not configured\" };\n        }\n\n        // Check if there are any players online first\n        const players = await getOnlinePlayers(gameServerId);\n        \n        // Skip sending message if no players are online\n        if (players.count === 0) {\n            console.log(\"No players online, skipping status message\");\n            return { success: true, reason: \"No players online\" };\n        }\n\n        // Use a fixed server start time for demonstration\n        const serverStartTime = new Date(\"2025-04-12T11:54:01.547Z\");\n\n        // Get the complete server status message\n        const message = await getServerStatusMessage(gameServerId, serverStartTime);\n\n        // Send the message to Discord\n        await takaro.discord.discordControllerSendMessage(discordChannel, {\n            message: message\n        });\n\n        // Return success result\n        return { success: true };\n    } catch (error) {\n        console.error(\"Error in serverStatus cronjob:\", error);\n        return {\n            success: false,\n            reason: error.message || \"Unknown error in serverStatus cronjob\"\n        };\n    }\n}\n\nawait main();","name":"serverStatus","description":"Why","temporalValue":"*/30 * * * *"}],"functions":[{"function":"import { takaro, data } from '@takaro/helpers';\n\n\n/**\n * Gets the server start time from the latest server-status-changed event with status \"online\"\n * @param {string} gameServerId - The ID of the game server\n * @returns {Promise<Date>} The server start time\n */\nexport async function getServerStartTime(gameServerId) {\n    try {\n        // First try to get server-status-changed events\n        const statusEvents = await takaro.event.eventControllerSearch({\n            filters: {\n                eventName: ['server-status-changed'],\n                gameserverId: [gameServerId]\n            }\n        });\n\n        // Filter for online events and find the most recent one\n        if (statusEvents.data.data.length > 0) {\n            const onlineEvents = statusEvents.data.data.filter(event =>\n                event.meta && event.meta.status === 'online'\n            );\n\n            if (onlineEvents.length > 0) {\n                // Sort by timestamp (newest first)\n                onlineEvents.sort((a, b) =>\n                    new Date(b.meta.timestamp).getTime() - new Date(a.meta.timestamp).getTime()\n                );\n\n                return new Date(onlineEvents[0].meta.timestamp);\n            }\n        }\n\n        // If no server-status-changed events found, try gameserver-created as fallback\n        const creationEvents = await takaro.event.eventControllerSearch({\n            filters: {\n                eventName: ['gameserver-created'],\n                gameserverId: [gameServerId]\n            }\n        });\n\n        if (creationEvents.data.data.length > 0) {\n            // Just use the first one since we're not sorting\n            return new Date(creationEvents.data.data[0].meta.timestamp);\n        }\n\n        // Final fallback - use server creation time from the gameServer object\n        const serverInfo = await takaro.gameserver.gameServerControllerGetOne(gameServerId);\n        if (serverInfo && serverInfo.data && serverInfo.data.data) {\n            return new Date(serverInfo.data.data.createdAt);\n        }\n\n        // If all else fails, use 1 day ago as fallback\n        const fallbackTime = new Date();\n        fallbackTime.setDate(fallbackTime.getDate() - 1);\n        return fallbackTime;\n    } catch (error) {\n        console.error(\"Error getting server start time:\", error);\n        // Return a fallback value if there's an error\n        const fallbackTime = new Date();\n        fallbackTime.setDate(fallbackTime.getDate() - 1);\n        return fallbackTime;\n    }\n}\n\n\n/**\n * Gets the server creation time from gameserver-created event\n * @param {string} gameServerId - The ID of the game server\n * @returns {Promise<Date>} The server creation time\n */\nexport async function getServerCreationTime(gameServerId) {\n    try {\n        // Get gameserver-created events\n        const creationEvents = await takaro.event.eventControllerSearch({\n            filters: {\n                eventName: ['gameserver-created'],\n                gameserverId: [gameServerId]\n            }\n        });\n\n        if (creationEvents.data.data.length > 0) {\n            // Sort by timestamp (oldest first) to get the original creation time\n            creationEvents.data.data.sort((a, b) =>\n                new Date(a.meta.timestamp).getTime() - new Date(b.meta.timestamp).getTime()\n            );\n\n            return new Date(creationEvents.data.data[0].meta.timestamp);\n        }\n\n        // Fallback to the server's creation time in the database\n        const serverInfo = await takaro.gameserver.gameServerControllerGetOne(gameServerId);\n        if (serverInfo && serverInfo.data && serverInfo.data.data) {\n            return new Date(serverInfo.data.data.createdAt);\n        }\n\n        // If all else fails, use today as fallback\n        return new Date();\n    } catch (error) {\n        console.error(\"Error getting server creation time:\", error);\n        return new Date(); // Return current time as fallback\n    }\n}\n\n\n/**\n * Calculates server uptime from a given start time\n * @param {Date} serverStartTime - The time when the server started\n * @returns {string} Formatted uptime string (e.g. \"2d 5h 30m\")\n */\nexport function calculateUptime(serverStartTime) {\n    try {\n        const currentTime = new Date();\n        const uptimeMs = currentTime.getTime() - serverStartTime.getTime();\n        const uptimeSeconds = Math.floor(uptimeMs / 1000);\n        const days = Math.floor(uptimeSeconds / 86400);\n        const hours = Math.floor((uptimeSeconds % 86400) / 3600);\n        const minutes = Math.floor((uptimeSeconds % 3600) / 60);\n\n        let serverUptime = \"\";\n        if (days > 0) serverUptime += `${days}d `;\n        if (hours > 0 || days > 0) serverUptime += `${hours}h `;\n        serverUptime += `${minutes}m`;\n\n        return serverUptime;\n    } catch (error) {\n        console.error(\"Error calculating server uptime:\", error);\n        return \"Unknown\";\n    }\n}\n\n\n/**\n * Gets a list of online players\n * @param {string} gameServerId - The ID of the game server\n * @returns {Promise<Object>} Object containing player count and formatted player list\n */\nexport async function getOnlinePlayers(gameServerId) {\n    try {\n        const onlinePlayers = await takaro.playerOnGameserver.playerOnGameServerControllerSearch({\n            filters: {\n                gameServerId: [gameServerId],\n                online: [true]\n            }\n        });\n\n\n        let playerList = \"No players online\";\n        const playerCount = onlinePlayers.data.meta.total;\n\n\n        if (playerCount > 0) {\n            const playerNamePromises = onlinePlayers.data.data.map(async (pog) => {\n                const playerData = await takaro.player.playerControllerGetOne(pog.playerId);\n                return playerData.data.data.name;\n            });\n\n\n            const playerNames = await Promise.all(playerNamePromises);\n            playerList = playerNames.map(name => `- ${name}`).join(\"\\n\");\n        }\n\n\n        return {\n            count: playerCount,\n            list: playerList\n        };\n    } catch (error) {\n        console.error(\"Error getting online players:\", error);\n        return {\n            count: 0,\n            list: \"Error retrieving player information\"\n        };\n    }\n}\n\n\n/**\n * Formats a date as a relative time string (e.g., \"2 days ago\")\n * @param {Date} date - The date to format\n * @returns {string} Formatted relative time string\n*/\nexport function formatRelativeTime(date) {\n    const now = new Date();\n    const diffMs = now.getTime() - date.getTime();\n    const diffSeconds = Math.floor(diffMs / 1000);\n    const diffMinutes = Math.floor(diffSeconds / 60);\n    const diffHours = Math.floor(diffMinutes / 60);\n    const diffDays = Math.floor(diffHours / 24);\n\n\n    if (diffDays > 0) {\n        return `${diffDays} day${diffDays !== 1 ? 's' : ''} ago`;\n    } else if (diffHours > 0) {\n        return `${diffHours} hour${diffHours !== 1 ? 's' : ''} ago`;\n    } else if (diffMinutes > 0) {\n        return `${diffMinutes} minute${diffMinutes !== 1 ? 's' : ''} ago`;\n    } else {\n        return 'just now';\n    }\n}\n\n\n/**\n * Generates a complete server status message\n * @param {string} gameServerId - The ID of the game server\n * @param {Object} config - User configuration options\n * @returns {Promise<string>} Formatted server status message for Discord\n */\nexport async function getServerStatusMessage(gameServerId, config = {}) {\n    try {\n        let message = \"# Server Status\\n\";\n\n        // Show player info only\n        const players = await getOnlinePlayers(gameServerId);\n\n        // Modified player info formatting\n        if (players.count > 0) {\n            message += `👥 **Players online:** ${players.count}\\n${players.list}`;\n        } else {\n            message += `👥 **Players online:** ${players.count}`;\n        }\n\n\n        return message;\n    } catch (error) {\n        console.error(\"Error generating server status message:\", error);\n        return `# Server Status\\nError: Could not retrieve server information`;\n    }\n}\n","name":"utils","description":null}],"permissions":[],"shopActions":[]}],"takaroVersion":"0.7.3"}